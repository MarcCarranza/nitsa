export type EventData = {
  day: string;
  place: string;
  name: string;
  artists: string[];
};

export type BgOptions = Record<string, boolean>;

export type Option = {
  label: string;
  value: number;
};
